#include <stdio.h>

/**
 * main - Prints alphabet in lowewrcase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)

		putchar(i);

	putchar('\n');
	return (0);
}
